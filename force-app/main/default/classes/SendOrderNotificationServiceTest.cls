public with sharing class SendOrderNotificationServiceTest {
    //Setup test Orders and Account 
    //Call Invocable method with order ids
    //Expect ids on sync call and get call out uses with Limits class to see that api has been called
}